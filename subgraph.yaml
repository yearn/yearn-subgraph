specVersion: 0.0.2
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum/contract
    name: yUSDVault
    network: mainnet
    source:
      address: "0x5dbcf33d8c2e976c6b560249878e6f1491bca25c"
      abi: Vault
      startBlock: 10559448
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.4
      language: wasm/assemblyscript
      entities:
        - Vault
        - VaultUpdate
        - Deposit
        - Withdrawal
        - Transfer
        - Account
        - AccountUpdate
        - AccountVaultBalance
        - Token
      abis:
        - name: Vault
          file: ./abis/Vault.json
        - name: ERC20
          file: ./abis/ERC20.json
      eventHandlers:
        - event: Transfer(indexed address,indexed address,uint256)
          handler: handleTransfer
      callHandlers:
        - function: deposit(uint256)
          handler: handleDeposit
        - function: withdraw(uint256)
          handler: handleWithdrawal
      file: ./src/mappings/vault.ts
# TODO: refactor vault as template
# TODO: add controller
# TODO: add strategy

# DEBUG: List of all entities
# Vault
# VaultUpdate
# AccountUpdate
# Account
# AccountVaultBalance
# Token
# Transfer
# Deposit
# Withdrawal
# StrategyUpdate
# Harvest
# Strategy
# Controller
